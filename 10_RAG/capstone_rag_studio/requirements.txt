# RAG Studio — backend + rag package dependencies (Python 3.11).
# Versions pinned to the known-good set verified across course notebooks 01-10.

# --- Web / transport ---
fastapi>=0.115
uvicorn>=0.50
python-multipart>=0.0.9
python-dotenv>=1.0

# --- Core / data ---
numpy>=1.26,<2.0
pydantic>=2.10
openai>=1.50

# --- Ingestion / parsing ---
langchain>=1.0
langchain-community>=0.4
langchain-text-splitters>=1.0
langchain-openai>=1.0
langchain-experimental>=0.4
pymupdf4llm>=1.28
rapidocr-onnxruntime>=1.4

# --- Embeddings / retrieval ---
sentence-transformers>=5.0
bm25s>=0.3
PyStemmer>=2.2

# --- Vector stores ---
chromadb>=1.5
faiss-cpu>=1.9
pinecone>=9.0          # optional, key-gated

# --- Reranking ---
FlashRank>=0.2
cohere>=7.0            # optional, key-gated

# --- Evaluation ---
ragas>=0.4
deepeval>=4.0
datasets>=3.0          # used directly by rag/evaluation.py to build the RAGAS dataset

# --- Testing ---
pytest>=9.0
pytest-xdist>=3.8
hypothesis>=6.100
httpx>=0.28            # FastAPI TestClient
