# Theme for the Production RAG Chatbot + Memory app.
# Matches the course's Marp deck palette (dark navy background, sky-blue accent)
# so the app and the theory slides read as one product line.

[theme]
base = "dark"
primaryColor = "#38bdf8"
backgroundColor = "#0f172a"
secondaryBackgroundColor = "#151f34"
textColor = "#e7ecf3"
