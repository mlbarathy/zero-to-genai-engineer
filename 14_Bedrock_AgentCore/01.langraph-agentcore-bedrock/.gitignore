# Local secrets / generated AWS resource credentials (NEVER commit)
.env
.env.*
!.env.example
gateway-credentials.json
*.pem
*.key

# Trainer-only (not for student repo)
TEACHING_SESSION_GUIDE.md

# Python / uv
.venv/
__pycache__/
*.pyc
.python-version
uv.lock
dist/
build/
*.egg-info/

# AgentCore local artifacts / build cache (regenerated by agentcore)
.bedrock_agentcore/
.bedrock_agentcore.yaml
.build_stamp
.dockerignore
Dockerfile
