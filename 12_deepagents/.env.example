# Copy to `.env` in this folder, or reuse `11_LangGraph/.env` / `10_RAG/.env`.

OPENAI_API_KEY=sk-your-openai-key-here

# Optional — LangSmith traces (same project name as the notebook).
LANGSMITH_TRACING=false
LANGSMITH_API_KEY=lsv2_your-langsmith-key-here
LANGSMITH_PROJECT=s12-deepagents
